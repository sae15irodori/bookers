<h1>Editing Book</h1>

<%= form_with model: @book,url: book_path(@book.id), method: :put do |f| %><!--editアクションで取得した編集するidﾃﾞｰﾀをfに格納-->

  <%= f.text_field :title %>
  <%= f.text_area :body %>
  
  <%= f.submit 'Update Book' %>
  
  <% end %>
  
  editのリンク indexへのリンク